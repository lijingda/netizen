"""HTTP errors shared by Admin request parsing and routing."""


class AdminWebError(RuntimeError):
    def __init__(self, status: int, code: str, message: str) -> None:
        super().__init__(message)
        self.status = status
        self.code = code
        self.message = message
